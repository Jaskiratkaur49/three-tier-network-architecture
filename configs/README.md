# Device Configurations

This directory contains sanitized Cisco IOS configurations for the network devices used in the Three-Tier Enterprise Network Architecture project.

## Structure

- `core/` - Core Layer switch configurations
- `distribution/` - Distribution Layer switch configurations
- `access/` - Access Layer switch configurations

Sensitive credentials have been removed or replaced with `<REDACTED>`.
